/**
 * @description 导出默认配置（通用配置|（地图）主题配置|网络配置）
 */

import netWork from './net.config'
import setting from './setting.config'
import viewerConfig from './viewer.config'

export { setting, netWork, viewerConfig }
